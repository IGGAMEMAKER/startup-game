import productActions from '../../actions/product-actions';

import c from '../../constants';

export default {
  load : () => {
    // const idea = c.ideas.IDEA_WEB_HOSTING;
    //
    // productActions.createCompany({ idea, name: 'WWWEB HOSTING', companyId: 0 });
    // productActions.createCompany({ idea, isCompetitor: true, companyId: 1 });
    // productActions.createCompany({ idea, isCompetitor: true, companyId: 2 });
    // productActions.createCompany({ idea, isCompetitor: true, companyId: 3 });
  }
}
